{
  // Type-checks components/*.tsx against the pinned @vaadin/react-components.
  // The examples are never built or shipped as JS — the compiler is used purely
  // as a correctness check, because a wrong example teaches the wrong API.
  //
  // .design-sync/previews/*.tsx is in scope for the same reason: Vite strips
  // their types without checking them, so a preview whose props have drifted
  // from the example would only surface as a broken card.
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "moduleResolution": "bundler",
    "jsx": "react-jsx",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "exactOptionalPropertyTypes": false,
    "skipLibCheck": true,
    "noEmit": true,
    "types": []
  },
  "include": ["components/**/*.tsx", "components/**/*.ts", ".design-sync/previews/*.tsx"]
}
