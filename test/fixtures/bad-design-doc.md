# Fixture: must FAIL the design-doc check

Cites a property Aura renamed away and a helper class that never existed, alongside
valid ones so the check has to discriminate rather than reject everything.

Spacing: `--vaadin-gap-m`, `--vaadin-padding-l`. Colour: `--vaadin-text-color`.
Renamed: `--aura-primary-color`. Missing class: `.aura-accent-teal`.
Correctly forbidden and exempt: never use `--lumo-font-size-xs`.
